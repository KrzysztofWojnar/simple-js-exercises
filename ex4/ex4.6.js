//    Ex 4.6 Implement following function

/**
 * @typedef ShoppingEntry
 * @property {number} price
 * @property {number} quantity
 */

/**
 * Calculates how much all the items on the shopping list will cost
 * @param {Array<ShoppingEntry>} shoppingList in format DD-MM-YYYY (like 25-02-2023 for 15th February 2023)
 * @returns {number} Total amount to be paid for all products on the list. Incorrect values would be ignored.
*/

export function exercise(shoppingList) {
    return null;
}
